# Supabase — copy to .env.local and fill in.
# Without these the site runs on the local demo content provider (dev only).
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
# Storage bucket used by the Media Manager
VITE_SUPABASE_MEDIA_BUCKET=media
